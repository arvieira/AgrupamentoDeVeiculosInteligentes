package br.ufrj.nce.labnet.vehicleunit.message;

public enum MessageType {
    ACK,            // Acknowledgement (ACK)
    ANSWER,         // Resposta (RESP)
    INVITATION,     // Convite (CONV)
    KEEP_ALIVE     // KeepAlive
}
